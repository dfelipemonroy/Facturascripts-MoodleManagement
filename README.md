# MoodleManagement

Base plugin for FacturaScripts. No functionality included in this version.

## Requirements

- FacturaScripts >= 2025.6

## Installation

Copy the `MoodleManagement` folder into the FacturaScripts `Plugins` directory and enable it from the admin panel.
