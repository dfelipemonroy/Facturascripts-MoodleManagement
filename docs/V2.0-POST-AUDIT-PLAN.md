# V2.0 Post-Audit Plan — Fases 15/16/17/18

**Scope**: resolver los 91 findings de la 2ª iteración de auditoría (2026-04-17) + 4 items migrados desde `V2.1-BACKLOG.md`. Total **95 items**.

**Decisión**: se retrasa el tag `v2.0.0` para bundlear todas las severidades (CRÍTICO + HIGH + MEDIUM + LOW + INFO) en el mismo release, en lugar de diferir HIGH→v2.0.1 y MEDIUM→v2.1 como se había planeado originalmente.

**Rama base**: `v2.0-dev` · **Rama activa**: `v2.0/fase-15-post-audit-fixes`.

**Leyenda**:
- `- [ ]` pendiente · `- [x]` completada · `- [~]` en curso · `- [!]` bloqueada · `SKIP:JUSTIFIED` descartada con razón
- 🔴 CRÍTICO · 🟠 ALTO · 🟡 MEDIO · 🔵 BAJO · ⚪ INFO

**Distribución global**:

| Fase | Nombre | Items | Horas aprox |
|------|--------|------:|------------:|
| 15 | Bloqueantes v2.0 | 6 | 7 |
| 16 | HIGH | 18 | 35-40 |
| 17 | MEDIUM | 38 | 60-80 |
| 18 | LOW + INFO + migrated | 34 | 20 |
| **TOTAL** | | **96** | **~130-150** |

> Nota: 96 = 91 findings + 4 migrados + 1 follow-up (crecerá conforme avancemos).

---

## FASE 15 — Bloqueantes v2.0.0 (6 items)

Todos estos deben cerrar antes del tag `v2.0.0`. Branch: `v2.0/fase-15-post-audit-fixes`.

- [x] **F15.1** 🔴 **SEC-01** — `Lib/Security/TokenCipher.php:189-197`. Fallback `'mm-fallback-insecure-secret'` hardcoded cuando falta `FS_COOKIES_EXPIRE`. Permite recuperación offline de tokens. **Fix**: fail-closed con `throw \RuntimeException` + test unit que verifique. ~0.5 h. · `393cafc`
- [x] **F15.2** 🔴 **SEC-02** — `Controller/ListMoodleAuditLog.php` + `Controller/ListMoodleTrash.php`. No verifican `$user->admin` explícitamente; non-admin con permiso-lista puede enumerar IPs/acciones. **Fix**: guard `if (empty($user->admin)) { $this->setTemplate('Error/AccessDenied'); return; }` en `privateCore`. Regression test en `ControllerAuthRegressionTest`. ~0.5 h. · `7be5196`
- [x] **F15.3** 🔴 **SEC-03** — `Lib/MoodleClient.php:110`. Acepta `http://` para instancia; token viaja en body sobre HTTP plano. **Fix**: rechazar non-HTTPS salvo que `environment='development'`. Test unit con matrix http/https/mixed. ~0.5 h. · `80233ad`
- [x] **F15.4** 🔴 **FE-01** — `View/MoodleDashboard.html.twig:227,230,270,284,322,324`. `json_encode \| raw` sin flags HEX. `</script>` en label rompe contexto. **Fix**: serializar en el Controller/Twig helper con `JSON_HEX_TAG\|JSON_HEX_APOS\|JSON_HEX_QUOT\|JSON_HEX_AMP\|JSON_THROW_ON_ERROR`. ~1 h. · `1cc75ae`
- [x] **F15.5** 🔴 **FE-02** — `View/Tab/CourseContent.html.twig:199,314,586-598` + `View/Tab/CourseGroups.html.twig`. Idem FE-01 en payloads de módulos Moodle. **Fix**: idem + añadir grep linter CI que falle si aparece `json_encode.*\|.*raw` sin `JSON_HEX`. ~2 h. · `1cc75ae`
- [x] **F15.6** 🔴 **BE-04** — `Cron.php:509-534`. Reconciliation marca `unenrolled` en masa si `getEnrolledUsers` devuelve `[]` por error silencioso. **Fix**: distinguir empty-real vs empty-por-error con health probe (`core_webservice_get_site_info` precondición) + guard "si el course devuelve 0 enrolments pero existen enrolments FS pending → skip batch y log warning". ~2 h. · `12e1c2d`

**Cierre fase 15**:
- [ ] Tests verdes: `vendor/bin/phpunit` → 97+ pass, 0 fail
- [ ] PHPStan sin crecer baseline
- [ ] PHPCS limpio
- [ ] Tag `pre-fase-15` en el último commit
- [ ] Fast-forward `v2.0-dev` → rama fase 15
- [ ] Update `CHANGELOG.md` sección `[2.0.0]` con los 6 fixes

---

## FASE 16 — HIGH (18 items)

Branch propuesta: `v2.0/fase-16-high-severity`.

### Seguridad (5)

- [ ] **F16.1** 🟠 **SEC-04** — `Lib/Webhook/WebhookVerifier::resolveSecret` fallback a plaintext en error de decrypt = bypass HMAC. **Fix**: fail-closed + audit log del decrypt failure. ~0.5 h.
- [ ] **F16.2** 🟠 **SEC-05** — `Lib/Security/SignedUrl` sin versionado de clave, rotación = downtime. **Fix**: añadir `v` prefix en el payload, aceptar v1/v2 en verify, emitir siempre última. ~2 h.
- [ ] **F16.3** 🟠 **SEC-06** — `Controller/MoodleCertificatePdf::isAuthorised` solo chequea `idcontactofact`. **Fix**: expandir check a toda la lista de contactos del cliente + ownership por factura. ~1 h.
- [ ] **F16.4** 🟠 **SEC-07** — Wizard cookie sin firma HMAC = tamper-able. **Fix**: envolver en `SignedPayload::pack($data, $ttl)` con HMAC + timestamp. ~2 h.
- [ ] **F16.5** 🟠 **SEC-08** — Moodle WS: token va como `wstoken` en body. **Fix**: migrar a `Authorization: Bearer <token>` cuando el server soporte; fallback documentado. ~2 h.

### Backend (8)

- [ ] **F16.6** 🟠 **BE-01** — 27 controladores sin `declare(strict_types=1)`. **Fix**: barrido con `php-cs-fixer` regla `declare_strict_types`. ~3 h.
- [ ] **F16.7** 🟠 **BE-02** — `Lib/Moodle/RetryPolicy.php` + `Lib/Moodle/CircuitBreaker.php` son **dead code**. Workers fallan a la primera. **Fix**: integrar en `Lib/Moodle/HttpClient::callApi` (primary) + propagar a `MoodleClient::callApi` legacy. Tests con mock fault-injection. ~6 h.
- [ ] **F16.8** 🟠 **BE-03** — `EnrolmentWorker` + `OnboardingWorker` no son idempotentes. Retry = duplicados. **Fix**: `idempotency_key = sha256(idfactura|idlinea|iduser)` + tabla `moodle_idempotency_log` con TTL 24h. ~4 h.
- [ ] **F16.9** 🟠 **BE-05** — `callApi` no propaga fallos parciales en bulk WS (ej. enrol 10 users, 3 fallan, response 200 OK). **Fix**: parsear `exception`/`warnings` del response shape, throw `PartialFailureException` con el detalle. ~4 h.
- [ ] **F16.10** 🟠 **BE-07** — SQL crudo en `EditContacto` extensión para actualizar timestamps. **Fix**: extraer `Lib/Contact/ContactTimestampUpdater` con interfaz limpia. ~2 h.
- [ ] **F16.11** 🟠 **BE-08** — Orphan cleanup itera N+1 queries. **Fix**: single-SQL `DELETE FROM moodle_user_map WHERE idcontacto NOT IN (SELECT idcontacto FROM contactos)` con guard `LIMIT` y chunking si > 1000. ~2 h.
- [ ] **F16.12** 🟠 **BE-09** — `BufferedLogger` crece sin límite en cron de larga duración. **Fix**: hard limit 10k entries + auto-flush + warning si se supera. ~1 h.
- [ ] **F16.13** 🟠 **BE-10** — `ListMoodleTrash::tableOf($type)` switch sin `default` throw. **Fix**: `throw new \InvalidArgumentException`. ~0.25 h.

### Base de datos (2)

- [ ] **F16.14** 🟠 **DB-01** — `TokenCipher::encryptExistingRows` sin transacción explícita. Si falla a mitad, mezcla de filas encriptadas + plaintext. **Fix**: `Tools::dbBegin() / dbCommit() / dbRollback()`. ~1 h.
- [ ] **F16.15** 🟠 **DB-03** — Migration ordering F5.11 (widen column) → F5.12 (encrypt). Si se ejecutan OOO, truncate. **Fix**: enforce orden con `dependsOn` en `SchemaMigrator::addMigration()`. ~1 h.

### Integración Moodle (1)

- [ ] **F16.16** 🟠 **INT-01** — Webhook payloads sin validación schema. Datos sucios rompen handlers. **Fix**: `Lib/Webhook/PayloadValidator` con JSON schema por event type (4 schemas). ~3 h.

### Frontend (2)

- [ ] **F16.17** 🟠 **FE-03** — `striptags` regex-based permite bypass trivial. **Fix**: usar `HtmlSanitizer::toPlainText()` (backed por `HTMLPurifier`). ~2 h.
- [ ] **F16.18** 🟠 **FE-04** — Helpers JS `setHtml(el, html)` llaman a `innerHTML`. **Fix**: migrar a `setText()` + DOM-create helpers para casos con markup requerido. ~3 h.

**Cierre fase 16**:
- [ ] Tests verdes + suite nueva para RetryPolicy/CircuitBreaker/PayloadValidator/Idempotency
- [ ] Tag `pre-fase-16` · fast-forward a `v2.0-dev`
- [ ] CHANGELOG updated

---

## FASE 17 — MEDIUM (38 items)

Branch propuesta: `v2.0/fase-17-medium-severity`.

### Seguridad MEDIUM (6)

- [ ] **F17.1** 🟡 **SEC-09** — HtmlSanitizer scheme match case-sensitive. Bypass con `JaVaScRiPt:`. **Fix**: normalizar `strtolower($scheme)` antes del allowlist check.
- [ ] **F17.2** 🟡 **SEC-10** — Rate limit bypass con `X-Forwarded-For` spoofing. **Fix**: trust-proxy list configurable; usar `REMOTE_ADDR` por defecto.
- [ ] **F17.3** 🟡 **SEC-11** — `RateLimiter` usa SHA-1 truncada (`STD-11` overlap). **Fix**: SHA-256 full + migration de cache keys.
- [ ] **F17.4** 🟡 **SEC-12** — CSP header solo en `MoodleCertificatePdf`. **Fix**: aplicar también a Dashboard + CourseContent (rutas con user content).
- [ ] **F17.5** 🟡 **SEC-13** — Password reset tokens sin rotation window. **Fix**: single-use + max 1h TTL.
- [ ] **F17.6** 🟡 **SEC-14** — Audit log sin hash chain. Permite alteración post-hoc. **Fix**: `prev_hash` columna + chain verification tool.

### Base de datos MEDIUM (11)

- [ ] **F17.7** 🟡 **DB-02** — `SoftDeleteTrait::delete()` asume PK simple. Tablas con PK compuesta fallan silencio. **Fix**: detectar PK compuesta + throw `UnsupportedSchemaException`.
- [ ] **F17.8** 🟡 **DB-04** — Trash regex sin anclas `^...$` → match parcial. **Fix**: anclar + unit test.
- [ ] **F17.9** 🟡 **DB-05** — `moodle_audit_log` + `moodle_webhook_log` append-only sin retention. Crecen indefinido. **Fix**: cron `logsRetention` (90d default, config).
- [ ] **F17.10** 🟡 **DB-06** — `idempotency_key` propuesto en BE-03 necesita índice único.
- [ ] **F17.11** 🟡 **DB-07** — `moodle_user_map.moodle_username` nullable sin índice. **Fix**: index + NOT NULL si post-onboarding.
- [ ] **F17.12** 🟡 **DB-08** — `created_at`/`updated_at` sin default server-side en 3 tablas. **Fix**: `DEFAULT CURRENT_TIMESTAMP`.
- [ ] **F17.13** 🟡 **DB-09** — FK `moodle_enrolments.idinstance` sin `ON DELETE CASCADE`. **Fix**: migration add cascade.
- [ ] **F17.14** 🟡 **DB-10** — PostgreSQL `LOWER(email)` vs MySQL `email` collation divergence. **Fix**: functional index o `LOWER()` en queries.
- [ ] **F17.15** 🟡 **DB-11** — `moodle_instances.url` sin `UNIQUE`. **Fix**: unique constraint post-dedup.
- [ ] **F17.16** 🟡 **DB-12** — `moodle_cohorts.codgrupo` FK nullable pero sin `ON DELETE SET NULL`. **Fix**: add.
- [ ] **F17.17** 🟡 **DB-13** — `CHARACTER SET`/`COLLATE` mix entre tablas core y plugin. **Fix**: migration normalize a `utf8mb4_unicode_520_ci`.

### Backend MEDIUM (1)

- [ ] **F17.18** 🟡 **BE-06** — Health check token quarantine no persiste fail-count. Flap constante. **Fix**: contador en `moodle_instances.health_fail_count` + threshold 3.

### Frontend MEDIUM (8)

- [ ] **F17.19** 🟡 **FE-05** — Scheme allowlist case-sensitive (overlap SEC-09).
- [ ] **F17.20** 🟡 **FE-06** — Chat error handler silencia 500s. **Fix**: toast + log.
- [ ] **F17.21** 🟡 **FE-07** — Dashboard TTL UX: cache miss = spinner forever. **Fix**: timeout 15s + retry button.
- [ ] **F17.22** 🟡 **FE-08** — Assets sin cache-busting. **Fix**: querystring `?v={{ plugin_version }}`.
- [ ] **F17.23** 🟡 **FE-09** — `escapeAttr` helper usa `htmlspecialchars` default. **Fix**: usar `ENT_QUOTES\|ENT_SUBSTITUTE\|ENT_HTML5`.
- [ ] **F17.24** 🟡 **FE-10** — Confirm dialog mix `confirm()` nativo + Bootstrap modal. **Fix**: unificar a Bootstrap modal promise-based.
- [ ] **F17.25** 🟡 **FE-11** — i18n strings hardcoded en JS. **Fix**: inyectar via `window.MM_I18N` desde Twig.
- [ ] **F17.26** 🟡 **FE-12** — Textarea sin `maxlength` attribute. **Fix**: add + client-side counter.

### Integración MEDIUM (9)

- [ ] **F17.27** 🟡 **INT-02** — `downloadFile` log sin cap de size → log flood con archivos grandes. **Fix**: truncar a 1KB o solo hash.
- [ ] **F17.28** 🟡 **INT-03** — `progressSync` no fuerza retry tras backoff persistente. **Fix**: force-retry flag tras 7d stuck.
- [ ] **F17.29** 🟡 **INT-04** — `ContactDelete` remap no bloquea si hay enrolments activos. **Fix**: guard.
- [ ] **F17.30** 🟡 **INT-05** — DNS rebinding en `IpValidator` post-DNS-lookup gap. **Fix**: resolver IP una vez + pasar a curl con `CURLOPT_RESOLVE`.
- [ ] **F17.31** 🟡 **INT-06** — `random_alias` probe no detecta colisiones transitorias. **Fix**: lookup pre-insert + retry.
- [ ] **F17.32** 🟡 **INT-07** — `UserApi::search` sin paginate-until-exhausted. **Fix**: loop con offset.
- [ ] **F17.33** 🟡 **INT-08** — `CourseApi::listCourses` cachea en memoria unbounded. **Fix**: LRU cap 500.
- [ ] **F17.34** 🟡 **INT-09** — `badge_sync_needed` flag sin UI para disparar resync manual. **Fix**: botón en `EditMoodleUserMap`.
- [ ] **F17.35** 🟡 **INT-10** — `MoodleClient::callApi` no loguea correlation-id. **Fix**: propagar `X-Request-ID`.

### Arquitectura MEDIUM (2)

- [ ] **F17.36** 🟡 **ARCH-02** — Dos sistemas de error handling: exceptions vs `['exception' => ...]` array. **Fix**: decisión: exceptions; ripout del patrón array en 6 callsites.
- [ ] **F17.37** 🟡 **ARCH-04** — Enums `Lib/Enum/*` definidos pero no adoptados en modelos (comparisons siguen con literales). **Fix**: migrar `Model/MoodleEnrolment`, `Model/MoodleUserMap`, etc.

### Performance MEDIUM (1)

- [ ] **F17.38** 🟡 **PERF-01** — Dashboard overview fetch sincrónico bloqueante. **Fix**: mover a worker + poll async (overlap con F3 frontend polling).

**Cierre fase 17**:
- [ ] Tests verdes, nuevos tests de idempotency + schema versioning
- [ ] Tag `pre-fase-17` · fast-forward a `v2.0-dev`

---

## FASE 18 — LOW + INFO + migrated (34 items)

Branch propuesta: `v2.0/fase-18-low-info-polish`.

### LOW (22)

- [ ] **F18.1** 🔵 **FE-13** — Chart.js fallback cuando CDN down. **Fix**: self-host vendored fallback.
- [ ] **F18.2** 🔵 **FE-14** — Label notes truncadas sin tooltip. **Fix**: `title` attr con contenido completo.
- [ ] **F18.3** 🔵 **FE-15** — Search sin spinner mientras query > 500ms.
- [ ] **F18.4** 🔵 **ARCH-01** — God class `MoodleClient` (1993 LOC, 100+ static). **Nota**: migración completa es v2.1 (ARCH-01 tracker). En v2.0: documentar deprecation notice en docstring + marcar `@deprecated since 2.0`.
- [ ] **F18.5** 🔵 **ARCH-03** — Controllers 1000+ LOC (`EditMoodleCourseMap`, `MoodleImportWizard`). **Nota**: extract `Lib/Action/*` es v2.1. En v2.0: docstring `@todo v2.1` + cobertura de tests en los métodos críticos.
- [ ] **F18.6** 🔵 **ARCH-05** — `ProductoMoodleDecorator` mal nombrado (no decorador real, sino extension). **Fix**: rename a `ProductoMoodleExtension` + alias deprecated.
- [ ] **F18.7** 🔵 **ARCH-06** — `BoundClient` churn: se instancia por request sin cache. **Fix**: memoize per-process.
- [ ] **F18.8** 🔵 **ARCH-07** — Extensions de FS core poco documentadas. **Fix**: README section.
- [ ] **F18.9** 🔵 **ARCH-11** — `BoundClient` cache invalidation al `reload`. **Fix**: hook al `Init::uninstall`.
- [ ] **F18.10** 🔵 **INT-11** — Token quarantine log sin context. **Fix**: añadir `instance_id`, `last_error`.
- [ ] **F18.11** 🔵 **PERF-02** — N+1 en `ListMoodleEnrolment::loadData`. **Fix**: eager load.
- [ ] **F18.12** 🔵 **PERF-03** — Seed benchmark 10k rows too slow. **Fix**: bulk insert.
- [ ] **F18.13** 🔵 **PERF-04** — `ContactSyncWorker` debounce por loop linear. **Fix**: hash-based.
- [ ] **F18.14** 🔵 **PERF-05** — PDF gen reloads template cada request. **Fix**: cache compiled.
- [ ] **F18.15** 🔵 **PERF-06** — Overview cache TTL demasiado corto (15s). **Fix**: 60s con invalidate on write.
- [ ] **F18.16** 🔵 **PERF-07** — JSON stream en export grande. **Fix**: `JsonMachine` o chunked.
- [ ] **F18.17** 🔵 **STD-01** — `declare(strict_types=1)` en `Worker/` + `Lib/` residuales (fuera de los 27 controllers de BE-01).
- [ ] **F18.18** 🔵 **STD-02** — `facturascripts.ini::min_version` 2025.6 probable too low. **Fix**: bump a 2025.8 + verify.
- [ ] **F18.19** 🔵 **STD-03** — Clases no-final que deberían ser final.
- [ ] **F18.20** 🔵 **STD-04** — PHPStan baseline strict-monotonicity rule documented.
- [ ] **F18.21** 🔵 **STD-05** — `LICENSE` dual MIT header inconsistente entre ficheros.
- [ ] **F18.22** 🔵 **STD-06** — Translation parity: `es_ES` 100%, otros idiomas gaps. **Fix**: script de parity check + fill-in keys missing con `es_ES` fallback.

### INFO (8)

- [ ] **F18.23** ⚪ **STD-07** — Test coverage < 30%. **Fix**: target 45% en v2.0; 60% en v2.1.
- [ ] **F18.24** ⚪ **STD-08** — `@since` tags incompletos.
- [ ] **F18.25** ⚪ **STD-09** — PHP-CS-Fixer rules subset.
- [ ] **F18.26** ⚪ **STD-10** — `.editorconfig` no cubre `.html.twig`. **Fix**: add.
- [ ] **F18.27** ⚪ **DOC-01** — `SECURITY.md` PGP fingerprint placeholder.
- [ ] **F18.28** ⚪ **DOC-02** — `docs/EVENTS.md` worker-order no documentado.
- [ ] **F18.29** ⚪ **DOC-03** — Webhook runbook faltante. **Fix**: `docs/WEBHOOK-RUNBOOK.md`.
- [ ] **F18.30** ⚪ **DOC-04** — `ARCHITECTURE.md` missing. **Fix**: extraer de este `CLAUDE.md` §3.
- [ ] **F18.31** ⚪ **DOC-05** — ADR directory missing. **Fix**: `docs/adr/` + adr-0001..0006 de CLAUDE.md §6.
- [ ] **F18.32** ⚪ **DOC-06** — Wizard storyline missing user guide.

### Migrados desde V2.1-BACKLOG.md (4)

- [ ] **F18.33** 🔵 **U4 (ex-v2.1)** — Inline cohort lifecycle filter usando `Lib/Enum/CohortLifecycle`. Enum ya existe, son ~10 LOC en `ListMoodleCohort`.
- [ ] **F18.34** 🔵 **S3 (ex-v2.1)** — `deleted_at` indexes en `moodle_user_map`, `moodle_enrolments`, `moodle_cohorts`. Migration.
- [ ] **F18.35** 🔵 **U1 (ex-v2.1)** — Twig-level `aria-label` pass. Runtime JS mirror ya existe (`a11y-enhance.js`); bakear atributos en templates.
- [ ] **F18.36** 🔵 **T4 (ex-v2.1)** — Bump PHPStan baseline a level 6 (strict-monotonicity).

**Cierre fase 18**:
- [ ] Tests verdes
- [ ] Tag `pre-fase-18` · fast-forward a `v2.0-dev`
- [ ] CHANGELOG consolidado con las 4 fases
- [ ] Actualizar `docs/V2.0-TASK-CHECKLIST.md` con total final (172 + 96 = 268 tareas trackables)
- [ ] Correr `RELEASE-CHECKLIST.md` completo
- [ ] Tag `v2.0.0`

---

## Notas de proceso

- **Commits**: Conventional Commits + audit ref. Formato: `<type>(v2.0): <subject> [#AUDIT-FX.Y-SEC-NN]`.
- **Tests nuevos obligatorios** para cada fix de severidad HIGH o superior.
- **Regresión cero**: cada fase arranca de tests verdes y cierra con tests verdes. Si una fase introduce regresión, se revierte o se bloquea hasta fix.
- **Dead code scope creep**: cada vez que se toque dead code (Retry/Breaker en BE-02, facades en ARCH-01), aprovechar para integrar en lugar de eliminar. Si no se puede integrar en v2.0, eliminar con justificación en CHANGELOG.
- **Orden preferente dentro de fase**: primero los IDs con menor superficie (changelog simple, un archivo), después los transversales.

*Documento vivo. Actualizar `[ ]` → `[x]` al cerrar cada item, con SHA del commit de cierre.*
